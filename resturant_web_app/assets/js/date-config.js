if ($(".booking-arrival")[0]){
		var d_arrival = TinyDatePicker('.booking-arrival', {
			mode: 'dp-below',
		});
} 


if ($(".booking-departure")[0]){
    var d_departure = TinyDatePicker('.booking-departure', {
			mode: 'dp-below',
	});
}